{
  "name": "shop-classroom",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "dev": "npx -y wrangler dev"
  },
  "dependencies": {
    "@tosspayments/tosspayments-sdk": "^2.8.1",
    "bcryptjs": "^3.0.3"
  }
}

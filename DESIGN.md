# 참고 화면 기반 디자인 가이드

## 1. 조사 범위와 원칙

- 대상: `shop/references/screens/`의 캡처 3장
  - `home.png` — 상품 목록 (1106 × 797)
  - `detail.png` — 상품 상세 (1270 × 831)
  - `cart.png` — 장바구니 (1288 × 847)
- 조사일: 2026-08-29
- 아래 숫자는 **캡처의 실제 픽셀을 측정한 값**이다. 짐작으로 채운 값은 없다.
- 캡처에서 읽을 수 없는 값은 9장에 비워 두고 질문으로 남겼다.
- 화면 구현은 이 문서의 범위가 아니다.

### 측정 방법과 신뢰도

측정 전에 캡처가 그대로 믿을 만한지부터 확인했다.

- **배율 1:1 확인** — 캡처 안의 모든 1px 선이 안티에일리어싱 없는 단색 한 줄로
  나온다 (`cart.png` y=74 `#8a8a8a`, `detail.png` y=248 `#cccccc`,
  `cart.png` y=307 `#e5e7eb`). 브라우저 확대 상태였다면 2px로 번지거나 색이
  섞여 나온다. 모서리 반경도 3/2/1/0 정수 계단으로 떨어진다.
- **DPR 1 확인** — 글자 가장자리에 ClearType 서브픽셀 프린지(주황·청록)가
  남아 있다. 고배율 렌더링이면 회색조로 나온다.
- **길이·색** — 픽셀 좌표를 직접 읽었다. 오차 없음.
- **글자 크기** — 캡처는 글꼴 선언을 담고 있지 않다. 그래서 화면에 찍힌
  글자의 **잉크 폭과 높이**를 재고, 같은 문자열을 맑은 고딕으로 여러 크기에
  렌더링해 폭·높이가 일치하는 크기를 찾았다. **±1px 오차가 있다.**
  표에 측정 잉크 높이를 함께 적어 두었으니 나중에 검증할 수 있다.

### 이 문서에 적지 않은 것

참고 화면에는 있지만 `SHOP.md` 범위에 없는 기능이라 **값을 옮기지 않았다.**
색과 규격이 문서에 남아 있으면 쓸 자리를 찾게 되고, 그러면 없는 정보를
지어내게 된다.

- 별점·리뷰 수 (리뷰 기능 없음)
- 배송 문구·배송 배지·적립 배지 (해당 기능 없음)
- 할인율·취소선 원가 (`SHOP.md`: 할인 없음)
- 프로모션 배너 (진행 중인 프로모션 없음)
- 좌측 필터 사이드바·메가 메뉴 (브랜드·가격대 정보 없음, 분류는 네 개뿐)
- 참고 화면의 로고·브랜드 이름·상품 사진·문구

## 2. 디자인 방향

참고 화면은 1:1 비율 상품 사진을 조밀한 격자에 놓고, 상품명과 가격을 짧은
간격 안에 쌓는 한국형 커머스 목록이다. 흰 바탕에 얇은 회색 선, 파랑은
누를 수 있는 것에만, 빨강은 가격에만 쓴다.

사람이 만든 화면처럼 보이게 하기 위한 적용 원칙:

- **제목을 크게 만들지 않는다.** 목록의 분류 제목도 측정값이 20px다.
  상품명은 12px, 가격은 14px다. 이 크기가 "실제 쇼핑몰"의 밀도를 만든다.
- **한 줄에 4개.** 콘텐츠 900px를 225px씩 4열로 나눈다. 카드를 크게 만들고
  한 줄에 3개를 놓으면 그 순간 화면이 AI가 만든 것처럼 보인다.
- **모든 블록을 둥근 카드에 넣지 않는다.** 목록의 상품 카드에는 테두리도
  그림자도 없다 (6장 참고). 곡률은 컨트롤에만 4px로 준다.
- **장식용 그라데이션과 그림자를 쓰지 않는다.** 참고 화면 3장 어디에도
  그라데이션이 없고, 측정된 그림자가 하나도 없다.
- **정보 순서를 지킨다.** 사진 → 상품명 → 가격. 그 사이를 장식으로 메우지
  않는다. 참고 화면에서 뺀 줄(리뷰·배송·적립)의 자리는 그냥 비운다.
- **가격은 빨강, 나머지 글자는 남색 계열 먹색.** 순검정(`#000000`)은 목록의
  분류 제목에만 쓴다.

## 3. 글꼴

### 글꼴 가족

캡처는 이미지라서 **글꼴 이름을 담고 있지 않다.** 글자 모양과 폭을 맑은 고딕
렌더링과 대조했을 때 일치했으나, 이것만으로 웹폰트 여부까지 단정할 수 없다.
9장의 질문으로 남긴다.

한글 UI 기본값으로 시스템 글꼴 스택을 쓴다. 새 글꼴을 임의로 불러오지 않는다.

```
-apple-system, "Malgun Gothic", "맑은 고딕", sans-serif
```

### 크기와 굵기

`측정 잉크`는 캡처에서 실제로 잰 글자 높이다. `크기`는 거기서 역산한 값이며
±1px 오차가 있다.

| 화면 | 용도 | 크기 | 굵기 | 측정 잉크 | 색 |
|---|---|---:|---:|---:|---|
| 목록 | 분류 제목 | `20px` | `700` | h 19 / w 35 (`식품`) | `#000000` |
| 목록 | 상품명 | `12px` | `400` | h 12 / w 142 | `#232b35` |
| 목록 | 판매가 | `14px` | `700` | h 13 / w 49 (`7,550원`) | `#ba2d1b` |
| 상세 | 상품명 | `13px` | | 폭 170 (높이 분리 불가) | `#232b35` |
| 상세 | 판매가 | | `700` | 분리 불가 | `#ba2d1b` 계열 |
| 장바구니 | 페이지 제목 | `34px` | `700` | h 37 / w 131 (`장바구니`) | `#131516` |
| 장바구니 | 상품명 | `16px` | `400` | h 15 / w 242 | `#232b35` |
| 장바구니 | 보조 정보 | `16px` | `400` | h 16 / w 144 | `#667380` |
| 장바구니 | 금액 | `28px` | `700` | h 24 / w 98 (`6,800원`) | `#232b35` |
| 장바구니 | 요약 제목 | `18px` | `700` | h 19 / w 117 | `#232b35` |
| 장바구니 | 요약 항목 라벨 | `16px` | `400` | h 15 / w 86 | `#474f5a` |
| 장바구니 | 요약 항목 값 | `16px` | `400` | h 15 | `#232b35` |
| 장바구니 | 합계 금액 | `20px` | `700` | h 20 | `#232b35` |

빈 칸은 캡처에서 확정하지 못한 값이다. 추정해 채우지 않았다.
상세 화면은 줄 간격이 좁아 글자 덩어리가 위아래로 붙어 있고, 상품명 오른쪽에
아이콘이 겹쳐 있어 높이를 따로 떼어 재지 못했다. 상품명 `13px`은 글자 폭
170px만으로 맞춘 값이라 다른 항목보다 근거가 약하다.

**목록 12px와 장바구니 16px는 오타가 아니다.** 참고 화면이 실제로 그렇다.
목록은 한 줄에 4개를 넣어야 해서 조밀하고, 장바구니는 한 줄에 하나라 넉넉하다.

## 4. 색상

### 측정된 색

| 색 | 역할 | 측정 위치 |
|---|---|---|
| `#ffffff` | 카드·콘텐츠 바탕 | 세 화면 공통 |
| `#f0f0f0` | 페이지 바탕 | `cart.png` 좌우 여백 전체 높이 |
| `#fafafa` | 정렬 바 바탕 | `home.png` y 363..395 |
| `#f2f4f6` | 비활성 버튼 바탕 | `cart.png` 비활성 주문 버튼 |
| `#131516` | 페이지 제목 | `cart.png` 장바구니 제목 |
| `#000000` | 분류 제목 | `home.png` 분류 제목 |
| `#232b35` | 기본 본문·금액 | 상품명, 장바구니 금액, 요약 값 |
| `#474f5a` | 요약 항목 라벨 | `cart.png` 요약 박스 좌측 라벨 |
| `#667380` | 3순위 보조 정보 | `cart.png` 상품 보조 줄 |
| `#acb5bf` | 비활성 글자·아이콘 | 비활성 버튼 글자, 비활성 수량 감소 |
| `#4269f6` | 누를 수 있는 것 | 활성 탭 표시, 아웃라인 버튼 선·글자 |
| `#ba2d1b` | 목록·상세 판매가 | `home.png` 네 카드 전부 |
| `#c6cdd4` | 컨트롤 테두리 | 수량 스테퍼, 상세 탭 하단선 |
| `#e0e3e7` | 박스 테두리·내부 구분선 | 요약 박스, 상세 옵션 행 사이 |
| `#e5e7eb` | 카드 테두리 | `cart.png` 상품 카드 |
| `#939eaa` | 체크박스 테두리 | `cart.png` 선택 체크박스 |

### 실제로 쓰는 색

위 16색을 그대로 쓰지 않는다. `#c6cdd4` · `#e0e3e7` · `#e5e7eb`는 눈으로
구분되지 않고, 큰 사이트가 오랫동안 쌓은 불일치일 뿐 설계된 계단이 아니다.
상품 8개짜리 화면에서는 지킬 수 없고, 매번 다른 것을 고르면 오히려 산만해진다.

**아래 12색만 쓴다. 여기 없는 색은 화면에 넣지 않는다.**

| 색 | 쓰는 곳 |
|---|---|
| `#ffffff` | 콘텐츠 바탕 |
| `#f0f0f0` | 페이지 바탕 |
| `#fafafa` | 분류 필터 바 바탕 |
| `#f2f4f6` | 비활성 버튼 바탕 |
| `#232b35` | 본문, 상품명, 금액, 활성 컨트롤 |
| `#474f5a` | 보조 라벨 |
| `#667380` | 3순위 보조 정보 |
| `#acb5bf` | 비활성 글자·아이콘 |
| `#e0e3e7` | 모든 선 — 테두리, 구분선 |
| `#939eaa` | 체크박스 테두리 |
| `#4269f6` | 누를 수 있는 것, 활성 상태 |
| `#ba2d1b` | 목록·상세 판매가 |

`#000000`과 `#131516`은 측정값이지만 `#232b35`와 역할이 겹친다. 제목도
`#232b35`로 통일한다.

## 5. 레이아웃과 여백

### 상품 목록

| 항목 | 값 | 근거 |
|---|---:|---|
| 콘텐츠 폭 | `900px` | `home.png` 배너 박스 x 188..1087 |
| 열 수 | `4` | 네 카드 확인 |
| 열 피치 | `225px` | 텍스트 좌측 210 / 435 / 660 / 885 |
| 카드 내부 좌우 여백 | `22px` | 열 시작 188 → 텍스트 210 |
| 카드 내용 폭 | `181px` | 225 − 22 × 2, 최장 상품명 폭 181과 일치 |
| 분류 제목 → 정렬 바 | | 사이에 프로모션 배너가 있어 측정 불가 |
| 정렬 바 높이 | `33px` | y 363..395 |
| 이미지 영역 아래 → 상품명 | `20px` | 이미지 영역 하단 598 → 상품명 618 |
| 상품명 → 판매가 | `10px` | 할인 없는 카드(3열) 상품명 629 → 가격 640 |

이미지 영역은 카드 내용 폭과 같은 `181px` 정사각형이다. 사진은 잘라내지 않고
정사각형 안에 여백을 두어 맞춘다 (`object-fit: contain`).

### 상품 상세

| 항목 | 값 | 근거 |
|---|---:|---|
| 이미지 영역 | `431 × 431` | 사진 잉크 y 151..581 |
| 정보 컬럼 좌측 경계 | `x = 573` | 아웃라인 버튼 좌측 선 |
| 정보 컬럼 폭 | `548px` | 아웃라인 버튼 x 573..1120 |
| 옵션 행 높이 | `약 59px` | 행 구분선 간격 (y 495 기준) |

### 장바구니

| 항목 | 값 | 근거 |
|---|---:|---|
| 좌 목록 폭 | `788px` | 카드 테두리 x 63..850 |
| 좌우 간격 | `12px` | 850 → 863 |
| 우 요약 박스 폭 | `375px` | 테두리 x 863..1237 |
| 합계 폭 | `1175px` | 788 + 12 + 375 |
| 요약 박스 내부 여백 | `25px` | 테두리 863 → 버튼 889 |
| 요약 박스 내부 구분선 간격 | `146px` / `141px` | y 210 → 356 → 497 |

## 6. 선, 테두리와 그림자

| 용도 | 측정값 | 이 프로젝트에서 쓸 값 |
|---|---|---|
| 장바구니 카드 테두리 | `1px solid #e5e7eb` | `1px solid #e0e3e7` |
| 요약 박스·내부 구분선 | `1px solid #e0e3e7` | `1px solid #e0e3e7` |
| 수량 선택기·상세 탭 하단선 | `1px solid #c6cdd4` | `1px solid #e0e3e7` |
| 아웃라인 버튼 | `1px solid #4269f6` | 그대로 |
| 체크박스 | `1px solid #939eaa` | 그대로 |
| 그림자 | **측정된 것 없음** | 쓰지 않는다 |

선 색 세 가지는 눈으로 구분되지 않으므로 `#e0e3e7` 하나로 합친다.
체크박스만 `#939eaa`를 유지한다. 선보다 진해야 클릭 대상으로 읽힌다.

**참고 화면 3장에서 그림자가 하나도 측정되지 않았다.** 목록의 상품 카드에는
테두리조차 없다. 상품을 떠 있는 카드처럼 만들지 않는다. 카드 테두리는
장바구니의 상품 행에만 있다.

## 7. 모서리

| 값 | 역할 | 근거 |
|---:|---|---|
| `0` | 목록 상품 카드, 이미지 영역 | 곡률 없음 확인 |
| `4px` | 버튼, 수량 스테퍼, 요약 박스, 장바구니 카드 | 세 요소 모두 3/2/1/0 계단으로 동일 |

**`4px` 하나만 쓴다.** 곡률을 두 종류 이상 섞지 않는다.

## 8. 컴포넌트 사양

### 상품 카드 (목록)

- 카드 폭 `225px`, 내부 좌우 여백 `22px`, 내용 폭 `181px`.
- 테두리 없음, 그림자 없음, 모서리 `0`.
- 이미지 영역 `181 × 181` 정사각형, `object-fit: contain`.
- 이미지 아래 `20px` 띄우고 상품명: `12px` / `400` / `#232b35`.
- 상품명 아래 `10px` 띄우고 판매가: `14px` / `700` / `#ba2d1b`.
- 상품명 아래에 리뷰·배송·적립 줄을 만들지 않는다. 그 자리는 비운다.

### 분류 필터

참고 화면의 정렬 탭(가로 텍스트 탭 줄)을 분류 필터의 본으로 삼는다.
좌측 필터 사이드바는 가져오지 않는다 (브랜드·가격대 정보 없음).

- 바 높이 `33px`, 바탕 `#fafafa`, 폭은 콘텐츠 폭과 같은 `900px`.
- 활성 항목 `#4269f6`, 비활성 항목 `#667380`.

### 버튼

| 종류 | 크기 | 바탕 | 글자 | 선 | 모서리 |
|---|---|---|---|---|---:|
| 아웃라인 | 폭 채움 × `33px` | `#ffffff` | `#4269f6` | `1px #4269f6` | `4px` |
| 비활성 | 폭 채움 × `55px` | `#f2f4f6` | `#acb5bf` | 없음 | `4px` |

**기본(강조) 버튼 사양은 참고 화면에 없다.** 장바구니 캡처의 주문 버튼이
비활성 상태로 찍혀 있어 활성 모습을 볼 수 없다. 9장 질문으로 남긴다.
값을 지어내지 말고 먼저 물어본다.

### 수량 선택기

- 크기 `140 × 40`, 테두리 `1px #e0e3e7`, 모서리 `4px`.
- 좌 `−` / 가운데 숫자 / 우 `+`.
- 활성 아이콘 `#232b35`, 비활성 아이콘 `#acb5bf`.
- 수량이 1이면 `−`를 비활성으로 표시한다 (참고 화면이 그렇게 되어 있다).
- `SHOP.md` 규칙대로 1 이상 99 이하로 제한한다.

### 장바구니 상품 행

- 카드 테두리 `1px #e0e3e7`, 모서리 `4px`.
- 체크박스 `30 × 30`, 테두리 `1px #939eaa`.
- 상품명 `16px` / `400` / `#232b35`.
- 보조 정보 `16px` / `#667380`.
- 금액 `28px` / `700` / `#232b35`. **장바구니 금액은 빨강이 아니다.**
  빨강은 목록과 상세의 판매가에만 쓴다.

### 주문 요약 박스

- 폭 `375px`, 테두리 `1px #e0e3e7`, 모서리 `4px`, 내부 여백 `25px`.
- 제목 `18px` / `700` / `#232b35`.
- 항목 줄 — 라벨 `16px` / `#474f5a`, 값 `16px` / `#232b35`.
- 항목과 합계 사이에 `1px #e0e3e7` 구분선.
- 합계 금액 `20px` / `700` / `#232b35`.
- 맨 아래 주문 버튼. 장바구니가 비어 있으면 비활성 사양을 쓴다.
- `SHOP.md` 범위에 배송비·쿠폰·할인이 없으므로 항목은 상품 금액과 합계뿐이다.

## 9. 확인되지 않아 비워 둔 값

| 항목 | 확인 질문 |
|---|---|
| 기본(강조) 버튼 — 바탕·글자색·높이 | 캡처의 주문 버튼이 비활성 상태로만 찍혀 있습니다. 활성 상태가 보이는 화면을 새로 캡처해 주시겠습니까? 아니면 `#4269f6` 바탕에 흰 글자로 정할까요? |
| 폼 입력 — 입력창·라벨·오류 문구 | 2단계 회원가입·로그인에 필요합니다. 캡처 3장에 입력 폼이 없습니다. 로그인 화면 캡처를 추가할까요? |
| 주문 완료 화면 | `SHOP.md` 1단계 화면인데 캡처가 없습니다. 장바구니 화면 규격을 그대로 적용할까요? |
| 빈 상태 (장바구니가 비었을 때) | 캡처가 담고 있지 않습니다. 문구와 모양을 정해 주시겠습니까? |
| 상품명 최대 줄 수와 높이 고정 여부 | 캡처의 상품명이 모두 한 줄이라 확인할 수 없습니다. 2줄 고정으로 가격 줄을 맞출까요? |
| 글꼴 이름 | 캡처는 이미지라 글꼴 선언을 담고 있지 않습니다. 시스템 글꼴로 갈까요, 특정 글꼴을 쓸까요? |
| 글자 크기 ±1px | 브라우저 개발자 도구의 계산값으로 재확인이 필요합니다. 확인해 주시겠습니까? |
| 상세 이미지 영역과 정보 컬럼 사이 간격 | 좌측 메가 메뉴가 펼쳐진 채 캡처되어 이미지 컬럼의 왼쪽 경계가 가려져 있습니다. 메뉴를 닫은 상태로 다시 캡처해 주시겠습니까? |
| 상세 화면의 글자 크기 전반 | 줄 간격이 좁아 글자 덩어리가 서로 붙어 있어 따로 재지 못했습니다. 상세는 목록·장바구니에서 확정한 크기를 준용할까요? |
| 페이지 전체 컨테이너 폭 | 장바구니 내부 폭 1175px은 측정했으나, 바깥 컨테이너의 좌우 여백이 좌우 비대칭(63 / 50)이라 확정하지 못했습니다. |
| 화면 폭이 900px보다 넓을 때 열 수 | 캡처가 4열입니다. 넓은 화면에서 5열로 늘릴까요, 900px 고정으로 둘까요? |
| 모바일 기준 폭과 breakpoint | 이번 범위에 모바일이 포함됩니까? |

## 10. 출처와 추적성

### 참고 화면

- `shop/references/screens/home.png` (1106 × 797)
  - 콘텐츠 폭, 4열 격자, 열 피치, 카드 내부 여백, 분류 제목, 정렬 바,
    상품명·판매가 크기와 색
- `shop/references/screens/detail.png` (1270 × 831)
  - 이미지 영역 크기, 정보 컬럼 경계와 폭, 옵션 행 구분선, 아웃라인 버튼
- `shop/references/screens/cart.png` (1288 × 847)
  - 페이지 바탕, 2단 구조와 폭, 카드·요약 박스 테두리와 모서리,
    수량 선택기, 비활성 버튼, 체크박스, 본문·보조·금액 색과 크기

### 검증 기록

- 세 캡처 모두 **배율 100% · DPR 1**로 확인했다. 1px 선이 단색 한 줄로
  떨어지고, 모서리 반경이 정수 계단(3/2/1/0)이며, 글자에 ClearType 서브픽셀
  프린지가 남아 있다. 따라서 캡처의 픽셀 = CSS 픽셀이다.
- 4열 × 225px = 900px이 프로모션 배너 박스 폭(x 188..1087 = 900px)과
  정확히 일치한다. 카드 내용 폭 181px도 캡처에서 가장 긴 상품명 폭 181px와
  일치한다. 격자 수치는 서로 교차 검증되었다.
- 모서리 반경 4px은 서로 다른 세 요소(비활성 버튼·수량 선택기·요약 박스)에서
  동일하게 측정되었다.

캡처에서 값을 읽지 못한 항목은 디자인 규칙으로 승격하지 않고 9장에 남겼다.

### 참고 화면을 바꾸고 싶을 때

새 캡처를 `shop/references/screens/`에 넣고 **이 문서를 다시 만든다.**
캡처만 바꾸고 문서를 그대로 두면 값이 어긋나 어느 쪽을 따를지 알 수 없게 된다.
